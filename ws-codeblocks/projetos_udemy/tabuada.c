#include <stdio.h>

int main(){

int  N, i;
int valor[i];

printf("Digite a tabuada a ser calculada : ");
scanf("%d", &valor[i]);

for(i=0; i=10; i++){
    printf("%d", (valor[i] * i));
}


return 0;
}
